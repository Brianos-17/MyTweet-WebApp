<div class="ui six column">
    <form class="ui stacked segment form" name="addTweet" action="/addTweet/{{user.email}}" method="POST">
        <div class="field">
            <label>Tweet</label>
            <input name="message" type="text" placeholder="What's on your mind?" maxlength="140">
        </div>
        <div class="field">
            <label>Image</label>
            <input name="image" type="file">
        </div>

        <button class="ui blue submit button"> Send </button>
    </form>
</div>